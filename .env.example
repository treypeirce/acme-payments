# Acme Payments API — environment template
# Copy to .env and adjust for local development.

PORT=8080
NODE_ENV=development

# HMAC secret used to sign API session tokens.
# In production this is injected from the secrets manager.
JWT_SECRET=dev-only-change-me-in-production
